<UserControl x:Class="NINA.Plugins.PlateSolvePlus.PlatesolveplusDockables.CameraDockableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Status -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="{Binding StatusText}" FontWeight="SemiBold" FontSize="14"/>
            <TextBlock Text="{Binding DetailsText}" TextWrapping="Wrap" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Camera Selection + Connection -->
        <GroupBox Header="Secondary Camera" Grid.Row="1" Margin="0,0,0,10">
            <StackPanel Margin="10">
<TextBlock Text="Select camera:" Margin="0,0,0,4"/>
<ComboBox ItemsSource="{Binding SecondaryCameraDevices}"
          SelectedItem="{Binding SelectedSecondaryCamera}"
          DisplayMemberPath="Name"
          Margin="0,0,0,10"/>

<WrapPanel Margin="0,0,0,8">
    <Button Content="Rescan ASCOM"
            Width="110"
            Margin="0,0,8,0"
            Command="{Binding RefreshSecondaryCameraListCommand}" />

    <Button Content="Driver Settings"
            Width="110"
            Margin="0,0,8,0"
            Command="{Binding OpenDriverSettingsCommand}" />

    <Button Content="Connect"
            Width="90"
            Margin="0,0,8,0"
            Command="{Binding ConnectSecondaryCommand}" />

    <Button Content="Disconnect"
            Width="90"
            Margin="0,0,8,0"
            Command="{Binding DisconnectSecondaryCommand}" />

    <Button Content="{Binding CaptureAndSolveButtonText}"
            Width="140"
            Margin="0,0,8,0"
            Command="{Binding CaptureAndSolveCommand}"
            IsEnabled="{Binding CanCaptureAndSolve}" />

    <Button Content="Calibrate Offset"
            Width="140"
            Command="{Binding CalibrateOffsetCommand}"
            IsEnabled="{Binding CanCalibrateOffset}" />
</WrapPanel>

<CheckBox Content="Capture and Slew"
          IsChecked="{Binding UseSlewInsteadOfSync}"
          Margin="0,0,0,8"/>


                <!-- Mount status -->
                <TextBlock Text="{Binding MountStatusText}"
                           Margin="0,0,0,6"
                           TextWrapping="Wrap"/>

                
                <TextBlock Text="{Binding MountDetailsText}"
                           Margin="0,0,0,6"
                           TextWrapping="Wrap"/><!-- Offset status -->
                <TextBlock Text="{Binding OffsetStatusText}"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </GroupBox>

        <!-- Capture parameters (anzeigen!) -->
        <GroupBox Header="Capture Parameters (from Options)" Grid.Row="2" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Exposure (sec)" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding GuideExposureSeconds}"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Gain" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding GuideGain}"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Binning" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding GuideBinning}"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Focal Length (mm)" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding GuideFocalLengthMm}"/>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Pixel Size (µm)" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding GuidePixelSizeUm}"/>
            </Grid>
        </GroupBox>

        <!-- Solve summary (nur hier anzeigen) -->
        <GroupBox Header="Solve Result" Grid.Row="3">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBox Grid.Row="0"
                         Text="{Binding LastSolveSummary}"
                         TextWrapping="Wrap"
                         IsReadOnly="True"
                         VerticalScrollBarVisibility="Auto"
                         Height="110"
                         Margin="0,0,0,8"/>

                <GroupBox Header="Captured Image" Grid.Row="1" Margin="0,0,0,8">
                    <Border Margin="10" BorderThickness="1" BorderBrush="#33000000">
                        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                            <Image Source="{Binding LastCapturedImage}" Stretch="None"/>
                        </ScrollViewer>
                    </Border>
                </GroupBox>

                <!-- optional: Platz für späteres Debug-Panel -->
                <TextBlock Grid.Row="2" Text="" />
            </Grid>
        </GroupBox>

    </Grid>
</UserControl>
